# 千里寻站点 · 阶段 1（P0 精准修复）交付说明

> 生成时间：2026-08-20
> 工作目录：`C:\Users\Administrator\WorkBuddy\千里寻\site-clone`（即 `Qianlixun/qianlixun` 浅克隆，commit `3a6572f`）

## 0. 执行环境说明（务必先读）

计划中原工作目录 `I:/千里寻/site-clone/` 在本环境不存在（无 `I:` 盘），已改为在本工作区克隆真实仓库进行修复。

本环境网络受限，导致两处与计划假设不同，已做对应调整：

| 计划假设 | 实际环境 | 处理方式 |
|----------|----------|----------|
| 工作目录 `I:/千里寻/site-clone/` 已含待改文件 | 无 `I:` 盘 | 直接克隆 `Qianlixun/qianlixun` 真实仓库修复 |
| 原主题 PNG/JPG 可从 jsdelivr/原仓库获取 | `cdn.jsdelivr.net/gh/chanshiyucx` 已 404；`github.com` 拉取不稳定（yoi 仓库克隆失败） | 装饰类图片一律生成为**自托管 SVG 占位**（见第 3 节），站点可正常运行，无死链 |
| jquery/backstretch 已下载到 `public/lib/` | `public/lib/` 不存在 | 从 `unpkg.com` 成功下载（JQ 88145B / BS 17490B） |
| 背景图换为免费图库 | bilibili 图床（`i0.hdslb.com`）国内可用且已成功拉取 | 19 张背景图已真实下载到 `public/assets/bg/` |

## 1. 修复清单（文件级）

| 文件 | 改动 |
|------|------|
| `vue.config.js` | 删除 `cdn.css`（Google Fonts）；`cdn.js` 改为本地 `/lib/jquery.min.js`、`/lib/jquery.backstretch.min.js` |
| `src/styles/variables.scss` | `$font-family-base` 改为系统字体栈（去 Fira Mono / Noto Serif SC）；`$mobile-bg`/`$aplayer-bg`/`$backtop-bg` 改为本地 SVG 路径 |
| `src/assets/images/index.js` | sakura/reimu/preview → 本地；bg 数组 → 本地下载的 19 张背景图 |
| `src/utils/index.js` | `fileCDN` 改为恒等函数（删除对原作者 raw→jsdelivr 的图床替换逻辑） |
| `src/config.js` | 14 处 jsdelivr URL → 本地路径；**删除原作者 QQ（顶部注释）与 telegram 联系项**；"主题配置文件"→"站点配置文件"；收款码标注待替换；头像改本地 |
| `src/views/Friend/index.vue` | "点击获取头像"链接的 jsdelivr 盗链 → 本地 `/assets/img/avatar.svg` |
| `public/index.html` | live2d 脚本加 `defer`；viewport 去除缩放限制（WCAG）；新增 OG / Twitter / JSON-LD 结构化数据；删除 jsdelivr 等外部 dns-prefetch |
| `package.json` | description 改为「千里寻 - 个人站点」 |
| `public/robots.txt`（新增） | Allow 全部 + Sitemap 声明 |
| `public/sitemap.xml`（新增） | 首页 + 各静态板块路由 |
| `.github/workflows/deploy.yml`（新增） | 源码仓库 push → 构建 → 部署到 `qianlixun.github.io`（DEPLOY_KEY） |

## 2. 构建与部署（在你本机执行）

```bash
cd site-clone
npm install          # 注意：仓库 .gitignore 忽略了 package-lock.json，用 install 而非 ci
npm run build        # Vue CLI 4 + webpack 4，在 Node 17+ 需：NODE_OPTIONS=--openssl-legacy-provider npm run build
# 产物在 dist/，本地预览：npx serve dist  或  npx http-server dist
```

部署（手动）：将 `dist/` 内容推送至 `qianlixun.github.io` 仓库 `master` 分支（或使用下方 CI 自动部署）。

## 3. 素材自托管与待替换项（重要）

**已真实落地（无需你处理）：**
- `public/lib/jquery.min.js`、`public/lib/jquery.backstretch.min.js`
- `public/assets/bg/*.jpg`（19 张背景图，真实图片）
- `public/live2d/`（看板娘本体，原本就自托管）

**当前为 SVG 占位，建议你替换以获得原版视觉效果：**
- 位置：`public/assets/img/*.svg`
- 包含：sakura / reimu / loading / defaultCover / cover2~5（音乐封面）/ email / github / music / twitter（联系图标）/ mobile（移动端背景）/ back-top（回顶）/ touhou·school（预览图）
- 替换方式：把真实图片放到同名位置（若用 `.png`/`.jpg`，需同步修改 `src/config.js`、`src/assets/images/index.js`、`src/styles/variables.scss` 中的引用路径）

**⚠️ 必须你本人提供（涉及隐私/收款）：**
- **支付宝 / 微信收款码** → 覆盖 `public/assets/img/qrcode-alipay.svg`、`qrcode-wechat.svg`（或放真实 png 并在 `src/config.js` 的 `qrcode[]` 改路径）
- **头像** → 当前为「千」字占位；如有自己的头像，覆盖 `public/assets/img/avatar.svg` 或在 `src/config.js` 的 `aboutOpts.avatar` 改路径

> 提示：原主题的装饰素材可由你在本机执行 `git clone https://github.com/chanshiyucx/yoi.git` 后，把 `blog/`、`bg/` 下的原图复制到 `public/assets/img/`（注意扩展名与上面引用一致）即可还原视觉。

## 4. CI 自动部署启用步骤

1. 在本机生成 deploy key：
   ```bash
   ssh-keygen -t ed25519 -C "gh-pages" -f deploy_key -N ""
   ```
2. 打开 `qianlixun.github.io` 仓库 → Settings → Deploy keys → Add key：
   - Title：`DEPLOY_KEY`，粘贴 `deploy_key.pub` 内容，**勾选 Allow write access**。
3. 打开 `Qianlixun/qianlixun` 仓库 → Settings → Secrets and variables → Actions → New repository secret：
   - Name：`DEPLOY_KEY`，粘贴 `deploy_key`（私钥）内容。
4. 推送 `master` 即自动构建并发布到 `qianlixun.github.io`。

## 5. 验证结果（完成定义）

- `grep -r "jsdelivr.net/gh/chanshiyucx" site-clone/` → **0** ✓
- `grep -r "fonts.googleapis.com" site-clone/` → **0** ✓
- `robots.txt` / `sitemap.xml` / `.github/workflows/deploy.yml` 均存在 ✓
- 原作者 QQ（1124590931）与 telegram（chan_shi_yu）残留 → **0** ✓
- 收款码占位已醒目标注「请替换」 ✓

## 6. 阶段 2（Vue3 + Vite 迁移）

见分支 `vue3-migration`（独立分支，本环境无法 `npm install` 构建验证，需你本机 `npm install && npm run build` 验收）。核心改动：`package.json` 依赖升级、`vite.config.js` 取代 `vue.config.js`、`main.js`/`router.js`/`store.js` 适配 Vue3 API、Vue2 专属依赖（`@moefe/vue-aplayer`、`vue-progressbar`）替换方案。详见该分支与迁移说明。
