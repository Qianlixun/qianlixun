# 千里寻站点 · 阶段 2（Vue2 → Vue3 + Vite 原地迁移）说明

> 分支：`vue3-migration`（基于阶段 1 P0 修复的提交切出，包含阶段 1 全部改动）
> 状态：**本环境无法 `npm install` / 构建验证，以下改动需你在本机验收**

## 1. 迁移策略

保留 SPA 形态与 Issues 数据层，原地平移到 Vue3 + Vite，不重写视图与数据层。

## 2. 改动文件一览

| 文件 | 改动 |
|------|------|
| `package.json` | 依赖升级：`vue@3` / `vue-router@4` / `vuex@4` / `vite` / `@vitejs/plugin-vue`；移除 `@vue/cli-*`；移除 Vue2 专属 `vue-progressbar`、`@moefe/vue-aplayer`；新增 `aplayer`、`nprogress` |
| `vite.config.js`（新增） | 取代 `vue.config.js`：`base:'/'`、`@` 别名、SCSS `additionalData` 全局注入 |
| `src/main.js` | `createApp`；`Vue.prototype` → `app.config.globalProperties`；`Vue.observable` → `reactive`；`Vue.use` → `app.use`；注册 APlayer 包裹组件 |
| `src/router.js` | `createRouter` + `createWebHistory`（取代 `new Router({mode:'history'})`） |
| `src/store.js` | `createStore`（取代 `new Vuex.Store`） |
| `src/App.vue` | 移除 `<vue-progress-bar />`；`$Progress` → `NProgress`；`beforeDestroy` → `beforeUnmount` |
| `src/components/ScrollTop/index.vue` | `$Progress.set` → `NProgress.set`；`beforeDestroy` → `beforeUnmount` |
| `src/components/Footer/index.vue` | `process.env.BASE_URL` → `import.meta.env.BASE_URL` |
| `src/components/MarkDown/index.vue` | `beforeDestroy` → `beforeUnmount` |
| `src/components/APlayer.vue`（新增） | 基于原生 `aplayer` 的包裹组件，替代 `@moefe/vue-aplayer` |
| `public/index.html` | 改为 Vite 入口（移除 EJS `<%= BASE_URL %>` 与 `<% for cdn %>` 模板，jquery/backstretch 以普通 `<script>` 引入） |

> 注：`vue.config.js` 仍保留在仓库但 Vite 不会读取，可忽略或删除。

## 3. 第三方依赖适配

- **顶部进度条**：`vue-progressbar`（Vue2） → `nprogress`（框架无关）。已引入 `nprogress/nprogress.css`，`App.vue`/`ScrollTop.vue` 调用 `NProgress.start()/set()/done()`。
- **音乐播放器**：`@moefe/vue-aplayer`（Vue2） → 原生 `aplayer` 包一层（`src/components/APlayer.vue`），在 `main.js` 全局注册为 `<APlayer>`，保持 `audio`/`fixed`/`mini`/`preload` 属性与 `update:mini` 事件。

## 4. 本机验证步骤

```bash
cd site-clone
git checkout vue3-migration
npm install                 # 仓库无 package-lock.json，用 install
npm run build              # Vite 构建，产物在 dist/
npm run preview            # 本地预览验证
```

## 5. 已知风险 / 需重点确认

1. **SCSS `@` 别名**：`vite.config.js` 的 `additionalData` 用 `@import "@/styles/variables.scss"`。Vite 5 一般可解析 `@` 别名；若构建报「找不到文件」，可改为相对路径或给 sass 配置 importer。
2. **APlayer mini 同步**：原生 `aplayer` 部分版本不一定派发 `updatemini` 事件，导致 Footer 的 `isMini` 状态可能与播放器内部不完全同步（仅影响「mini 态」样式，音乐播放不受影响）。
3. **AOS 初始化**：原项目仅引入 `aos/dist/aos.css`，未在本迁移范围内处理 `AOS.init()`（属历史遗留，非本次回归，如动画缺失请检查 AOS 初始化位置）。
4. **`marked` 版本**：保持 `^1.1.1`，继续依赖 `this.options.highlight`；请勿升级到 marked v5+（移除 highlight 钩子）。

## 6. 完成定义（功能清单逐项验证）

- [ ] 首页 / 文章页 / 归档 / 分类 / 标签 / 灵感 / 书单 / 友链 / 关于 均正常渲染
- [ ] 评论（Gitalk）正常
- [ ] 音乐播放器（APlayer）正常播放、mini 切换
- [ ] 看板娘（Live2D）加载、点击互动
- [ ] 主题配色切换（Theme.vue）正常
- [ ] 顶部进度条（nprogress）随路由切换

## 7. 上线顺序建议

1. 先在 `master` 合并并上线**阶段 1（P0 修复）**（已独立可部署、已验证）。
2. 本机通过阶段 2 构建验证后，再将 `vue3-migration` 合并到默认分支并切换。
